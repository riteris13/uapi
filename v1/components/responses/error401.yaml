description: Bad Request
headers:
  traceresponse:
    $ref: ../headers/traceresponse.yaml
content:
  application/json:
    schema:
      oneOf:
        - $ref: ../schemas/AuthorizedClientsOnly.yaml
        - $ref: ../schemas/InvalidToken.yaml