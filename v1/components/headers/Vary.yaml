description: |
  `Vary` is a header used to indicate which headers are used when selecting a representation of a resource in a content negotiation and determines how to match future request headers to decide whether a cached response can be used rather than requesting a fresh one from the origin server. To allow multilanguage content support `Accept-Language` tag is used.
required: true
schema:
  type: string
  examples:
    - Accept-Language